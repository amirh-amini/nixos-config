# Editor / AI-assistant state — keep local, never track
.vscode/
.claude/
CLAUDE.md
CLAUDE.local.md

# Nix / dev build artifacts
result
result-*
.direnv/

# Emacs: generated tangle output + auto-save/backup files
modules/home-manager/programs/emacs/config.el
\#*\#
.\#*
*~
